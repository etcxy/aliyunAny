from aliyunsdkcore.client import AcsClient

# 服务器位置
location = "cn-hangzhou"

# api id-key
client = AcsClient(
    "xxxxxxxxxxxxxx",
    "yyyyyyyyyyyyyyyyyyyyyyyyyy",
    location
)

# 网络安全组
security_group_id = "sg-xxxxxxxxx"

# 主控机key
key_pair = ""

# 交换机ID
vswitch_id = "vsw-zzzzzzzzzzzzzzzzzzzz"

# 私钥位置
id_rsa_position = ""


# hosts文件位置 (修改文件，需要管理员权限)
# hosts_position = "C:windows/system32/..."

# hosts_known位置(Linux系统需要)
hosts_known_position = ""

# 自定义主机配置
instance_machine_type = ""

# Windows需要此设置 其他平台设置为 utf-8
encode = "utf-8-sig"